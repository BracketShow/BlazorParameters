@typeparam TItem

<table class="table" style="background-color:@TableBackgroundColor.Name">
    <thead>
    <tr>@TableHeader</tr>
    </thead>
    <tbody>
    @foreach (var item in Items)
    {
        <tr>@RowTemplate(item)</tr>
    }
    </tbody>
</table>